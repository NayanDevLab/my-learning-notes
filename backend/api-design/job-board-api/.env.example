# Copy to .env and adjust. All have safe defaults so the app runs without this.
PORT=3000

# JWT secrets — generate strong random values in production!
ACCESS_SECRET=dev-access-secret-change-me
REFRESH_SECRET=dev-refresh-secret-change-me
